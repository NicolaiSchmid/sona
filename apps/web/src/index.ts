/**
 * @sona/web
 *
 * Review UI, onboarding, and document upload. Placeholder entrypoint for now.
 */

/** Package version marker, used to verify wiring and test discovery. */
export const sonaWebVersion = "0.0.0" as const;
